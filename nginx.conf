server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index OPS_Dashboard_Gestão_de_Operações.html;

    location / {
        try_files $uri $uri/ =404;
    }

    gzip on;
    gzip_types text/html text/css application/javascript;
}