# Dependências do Node
node_modules/

# Dados do servidor (não devem ir para o Git — são os dados reais da empresa)
server/data.json
server/data.backup.json
server/data.json.tmp

# Arquivos de sistema
.DS_Store
Thumbs.db

# Variáveis de ambiente
.env
